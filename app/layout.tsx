import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Pro Scout AI",
  description: "Elite Football Scouting & Player Analysis Powered by AI",
  openGraph: {
    title: "Pro Scout AI",
    description: "Generate professional football scouting reports in seconds.",
    siteName: "Pro Scout AI",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}